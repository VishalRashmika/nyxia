class UiConstants {
  // App Info
  static const String appName = 'NYXIA';
  static const String appTagline = 'An Astrophotography Planner App';

  // Loading Screen
  static const Duration loadingDuration = Duration(seconds: 3);
  static const double logoSize = 120.0;
  static const double progressBarWidth = 200.0;
  static const double progressBarHeight = 4.0;

  // Animation Durations
  static const Duration fadeInDuration = Duration(milliseconds: 800);
  static const Duration scaleInDuration = Duration(milliseconds: 600);
}
